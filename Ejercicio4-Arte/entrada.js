/**
 * Asigna el nombre de la pelicula/escultura/cuadro/obra
 * @param {string} nombre - indica el tipo de arte
 * @returns {string}
 */
function entrada(nombre) {
   /**
    * nombre de la pelicula/escultura/cuadro/obra
    * @type {string}
    */
   var entrada = prompt("Indica" + nombre)
   return entrada
}